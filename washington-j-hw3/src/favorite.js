class Favorite {
    constructor(fid, text, url, comments){ // `fid` stands for `favorites ID`
        this.fid = fid;
        this.text = text;
        this.url = url;
        this.comments = comments;
    }
}

export {Favorite};